Сайт больницы 

## Что нужно для запуска

1. PHP 8+.
2. MySQL
3. Web-сервер (Apache/Nginx) или встроенный PHP-server.
4. Папка для загрузок: `uploads/`.

## Настройка подключения к БД

1. Откройте `config/config.connection.php` и задайте:
   - `db.host`
   - `db.name`
   - `db.user`
   - `db.pass`
   - `db.charset` (обычно `utf8mb4`)

`includes/bootstrap.php` требует `config/config.php`.
В проекте он уже есть и собирает конфигурацию из `config.connection.php` (и, если доступен, из `config.mail.php`).

## Настройка почты (для уведомлений/обратной связи)

Уведомления отправляются через `config/config.mail.php` (SMTP).
Если файл не настроен/отсутствует — сайт в целом будет работать, но письма могут не уходить.

## Запуск

1. Поднимите сервер (из корня проекта):
   - `php -S localhost:8000`
2. Откройте в браузере:
   - `http://localhost:8000/index.php`
3. При первом запуске приложение создаст/обновит схему БД через `includes/db_schema_ensure.php`.

or

1. OperServer 

## Админ-панель (важно)

Доступ в админку включается полем `users.is_admin` (или ролью `users.role='admin'`).

Чтобы первый раз зайти в админку, включите суперадмина в вашей БД:

```sql
UPDATE users
SET is_admin = 1
WHERE email = 'admin@hospital.local';
```

Дальше управление пользователями/врачами — в:
- `admin/users.php`
- `admin/doctors.php`
- `admin/services.php`

## Страницы

- Главная: `index.php`
- Услуги: `pages/services.php`
- Кабинет пациента: `cabinet/`
- Кабинет врача: `doctor/`

## Функции сайта

### Публичная часть

- Главная страница, навигация по секциям
- Просмотр списка **услуг** (берётся из БД)
- Просмотр списка **врачей** (берётся из БД)
- Форма **обратной связи** (сообщение администратору)
- Страница услуг `pages/services.php` с динамической подгрузкой услуг из БД

### Пациент (личный кабинет)

- Регистрация и вход
- Просмотр профиля и редактирование (ФИО/телефон)
- Смена пароля
- Запись на приём (выбор врача/даты/времени)
- Отображение занятых слотов у врача как **«занято»** (недоступны для выбора)
- История записей
- Отмена записи (для статусов `pending/confirmed`)
- Получение уведомлений (внутри кабинета)

### Врач (кабинет врача)

- Вход в кабинет врача (для пользователя с ролью `doctor` и привязанным `doctor_profile_id`)
- Просмотр записей пациентов, которые записаны к этому врачу
- Отметка «пациент пришёл»
- Комментарий врача (виден пациенту в личном кабинете)

### Админ-панель

- Вход в админ-панель
- Управление врачами (`admin/doctors.php`): создание/редактирование и привязка аккаунта к врачу
- Управление услугами (`admin/services.php`)
- Обращения/анкеты в админке (`admin/notifications.php`) и ответы пользователям (с email‑уведомлением)
- Управление пользователями (`admin/users.php`, доступно только `admin@hospital.local`):
  - выдача/снятие админ-прав
  - список врачей и пользователей с админ-правами
  - редактирование пользователя (email/ФИО/телефон/роль/doctor_profile_id)
  - смена пароля пользователю (по желанию)
  - удаление пользователя

